<shape xmlns:android="http://schemas.android.com/apk/res/android">
  <!-- 배경 색 (없음으로 설정) -->
  <solid android:color="@android:color/transparent" />

  <!-- 테두리 정의 -->
  <stroke
      android:width="2dp"
      android:color="@color/yellow" /> <!-- 테두리 색상 (빨간색) -->

  <!-- 둥근 모서리 -->
  <corners android:radius="20dp" />
</shape>
